SYS=`uname -s`

if [ "$SYS" = "Linux" ]; then
	SYS=`cat /etc/os-release | grep "^ID=" | cut -d'=' -f2`
fi

case "$SYS" in
	arch) JAVA_HOME="/usr/lib/jvm/java-21-openjdk" ;;
	Darwin) JAVA_HOME="/Library/Java/JavaVirtualMachines/openjdk-21.jdk/Contents/Home" ;;
esac

export JAVA_HOME
