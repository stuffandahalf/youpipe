<html lang="en">
  <head>
    @if{!isReload || childTemplate == null}
      <title>YouPipe</title>
    @end{}

    <!-- common css rules -->
    <style>
      nav, nav form {
        display: inline;
      }
    </style>
  </head>
  <body hx-target="main">
    <nav>
      <a hx-get="/apis/user/feed">Feed</a>
      <a hx-get="/apis/user/playlists">Playlists</a>
      <a hx-get="/search">Search</a>
      <a hx-get="/settings">Settings</a>
    </nav>
    <main hx-history-elt>
      @if{isReload && childTemplate != null}
        @include{childTemplate}
      @else{}
        <p>Served from vert.x!</p>
      @end{}
    </main>
    <footer>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>
  </body>
</html>
